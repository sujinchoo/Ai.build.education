# Week 01 — 컴퓨터 원리와 Python 입문

## 주제
컴퓨터의 기본 동작 원리와 컴퓨팅 역사, 그리고 Python 실행 기초를 연결해 이해한다.

---

## 학습 목표
- 컴퓨터의 4대 기능(입력/처리/저장/출력)을 설명할 수 있다.
- 튜링 머신과 폰 노이만 구조가 현대 컴퓨터에 준 영향을 설명할 수 있다.
- Python 실행 구조, 변수, 기본 자료형을 사용해 간단한 코드를 작성할 수 있다.

---

## 학습 내용 (목표 연계)
- **컴퓨터 4대 기능 이해**: 입력(키보드/마우스), 처리(CPU), 저장(RAM/SSD), 출력(모니터/스피커)이 하나의 흐름으로 연결된다는 점을 사례로 익힌다.
- **튜링 머신과 폰 노이만 구조**: 튜링 머신은 ‘계산 가능한 문제’의 기준을 만들었고, 폰 노이만 구조는 코드와 데이터를 메모리에 함께 저장하는 현대 컴퓨터 구조의 표준이 되었다.
- **Python 실행 구조 기초**: Python 코드는 위에서 아래로 실행되며, 변수는 데이터를 이름으로 저장한다. 문자열(str), 정수(int), 실수(float)처럼 자료형에 따라 사용할 수 있는 연산이 달라진다.
- **초급자 포인트**: 에러가 나면 에러 메시지의 마지막 줄부터 읽고, 변수명 오타·괄호 누락·들여쓰기를 먼저 확인하는 습관을 만든다.

---

## 비주얼 콘셉트
입력 장치 → CPU 처리 → 메모리 저장 → 출력 장치

튜링 머신 개념 → 폰 노이만 구조 → 현대 범용 컴퓨팅

### 그림
```mermaid
flowchart LR
    I[입력: 키보드/마우스] --> C[처리: CPU]
    C --> M[저장: 메모리/스토리지]
    M --> O[출력: 모니터/스피커]
```

```mermaid
flowchart LR
    T[튜링 머신 1936] --> V[폰 노이만 구조 1945]
    V --> N[현대 컴퓨터/클라우드]
```

---

## 학습 예시 및 코드
- 컴퓨터는 **입력-처리-저장-출력**의 순환 구조로 동작한다.
- 현대 소프트웨어는 폰 노이만 구조(메모리에 코드와 데이터를 함께 저장) 위에서 실행된다.
- Python은 인터프리터 언어로, 작성한 코드를 위에서 아래로 순차 실행한다.

```python
user_name = "Mina"
score = 95
print(user_name, score)
print(type(user_name), type(score))
```

- 위 코드는 문자열(`str`)과 정수(`int`) 자료형을 출력하고 타입을 확인한다.
- 최신 개발 환경에서는 로컬 IDE + 클라우드 실행 환경(예: Codespaces, Colab)을 함께 사용한다.

---

## 핵심개념 정리
- 컴퓨터 구조: 입력, 처리, 저장, 출력
- 컴퓨팅 역사: 튜링 머신, 폰 노이만
- Python 기초: 변수, 자료형, 순차 실행

---

## 실습 미션
1. 이번 주 학습 목표 3가지를 확인하고, 각 목표를 검증할 수 있는 실습 항목을 최소 1개씩 수행한다.
2. 실습 과정(입력값, 코드/설정, 실행 결과)을 문서나 노트에 정리한다.
3. 어려웠던 점 1가지와 다음 주에 개선할 점 1가지를 작성한다.

---

## 확장 실습
- `input()`으로 사용자 입력을 받아 자기소개 한 줄 출력하기
- `f-string`으로 출력 포맷 개선하기

---

## 체크리스트
- [ ] 컴퓨터 4대 기능을 설명할 수 있다.
- [ ] 폰 노이만 구조를 한 문장으로 설명할 수 있다.
- [ ] Python 변수와 자료형을 코드로 표현할 수 있다.
